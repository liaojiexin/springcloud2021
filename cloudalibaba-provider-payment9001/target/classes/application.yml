server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #注册中心地址
        server-addr: localhost:8848


management:
  endpoints:
    web:
      exposure:
        include: "*"
