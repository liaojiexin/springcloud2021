server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #nacos注册中心
        server-addr: localhost:8848
        #nginx代理集群的地址
#        server-addr: localhost:8080


management:
  endpoints:
    web:
      exposure:
        include: "*"
